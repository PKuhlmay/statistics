# Normalize line endings to LF on commit
* text=auto eol=lf

# Binary files — never touch line endings
*.{png,jpg,jpeg,gif,ico,svg,woff,woff2,ttf,eot,otf,mp4,mp3,pdf,zip,gz} binary
pnpm-lock.yaml binary
